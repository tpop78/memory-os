{
  "name": "memory-os",
  "version": "0.5.0",
  "description": "Durable working memory for AI coding agents: bounded re-hydration across compaction and sessions. Auto-inits .memory/ loop + CodeGraph index on SessionStart (MEMORY_OS_AUTO_INIT, default on). /memory-init scaffolds the loop manually; context inventory prevents redundant reads; /auto-research runs an autonomous optimize loop on one asset toward one metric.",
  "author": {
    "name": "tpop78"
  }
}
