{
  "name": "memory-os",
  "version": "0.3.0",
  "description": "Durable working memory for AI coding agents: bounded re-hydration across compaction and sessions. /memory-init scaffolds the .memory/ loop; context inventory prevents redundant reads and session depth breakpoints optimise token usage on large projects.",
  "author": {
    "name": "tpop78"
  }
}
